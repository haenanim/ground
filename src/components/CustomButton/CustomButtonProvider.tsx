import React from 'react';
import CustomButton from './CustomButton';

interface props {
  children: any;
}
export default function CustomButtonProvider({ children }: props) {
  return <div></div>;
}
